# {项目名称} -- 开发计划

> 慢变架构设计文档。只记录目标、边界、核心模块、接口契约和关键设计决策；需求池、路线图、Sprint 任务不写在这里。

## 使用边界

- 默认不在 Sprint Planning 时全文读取。
- 只有涉及架构、接口契约、数据模型、关键依赖、系统边界时才读取相关章节。
- 普通需求、任务状态、Sprint 进度写入 `Backlog.md` 或 `任务清单.md`。

## 变更历史

| 日期 | 变更摘要 |
| --- | --- |
| {YYYY-MM-DD} | 初始版本 |

---

## 一、项目目标

### 目标

- {目标 1}
- {目标 2}

### 非目标

| 不做 | 原因 |
| --- | --- |
| {不做的事情} | {原因} |

---

## 二、系统边界

```text
{上游系统} --> [{项目名称}] --> {下游系统/协议}
```

| 方向 | 协议 / 方式 | 说明 |
| --- | --- | --- |
| 入 | {gRPC / HTTP / MQ} | {说明} |
| 出 | {gRPC / HTTP / MQ} | {说明} |

---

## 三、核心模块

| 模块 | 路径 | 职责 |
| --- | --- | --- |
| {模块名} | {路径} | {职责} |

---

## 四、接口契约

| 类型 | 名称 / 路径 | 请求 | 响应 | 说明 |
| --- | --- | --- | --- | --- |
| gRPC | {RPC 方法} | {XxxReq} | {XxxRes} | {说明} |
| API | {METHOD /path} | {XxxRequest} | {XxxResponse} | {说明} |

---

## 五、数据模型

| 名称 | 类型 | 关键字段 | 说明 |
| --- | --- | --- | --- |
| {模型名} | {struct / table / cache key} | {字段} | {说明} |

---

## 六、配置与依赖

| 配置 / 依赖 | 说明 | 默认值 / 来源 |
| --- | --- | --- |
| {配置项} | {说明} | {默认值} |

---

## 七、关键设计决策

| 决策 | 原因 | 影响 |
| --- | --- | --- |
| {设计决策} | {为什么这样做} | {影响范围} |

---

## AI 维护规则

- 开发启动遵循 `trellis:start`，本文件不是启动入口。
- 只有架构、接口契约、数据模型、关键依赖发生变化时才更新本文件。
- 更新前优先定位相关章节，不默认读取或改写全文。
- 重大架构变化时先保留旧版本为 `开发计划-v{N}.md`，再更新本文件。
