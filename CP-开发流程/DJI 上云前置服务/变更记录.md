# DJI 上云前置服务 -- 变更记录

> 按 Sprint 分组的变更归档文档，倒序排列（最新 Sprint 在最前面）。
> AI 每完成一个任务后立即追加记录，只追加不修改。

<!--
=== 变更记录格式 ===

每个 Sprint 按以下格式追加（插入到本注释下方，保持最新在前）：

## Sprint S{N}: {Sprint主题}

**日期**: {YYYY-MM-DD}
**关联 Story**: {B-001, B-002}

### S{N}-{序号}: {任务标题}
- **变更概述**: {一句话描述}
- **变更文件**:
  - `{文件路径}` -- {变更说明}
- **影响范围**: {受影响的服务/模块}
- **验证结果**: {编译/测试结果}

---
-->

## Sprint S8: Proto optional 清理 + SDK 补齐 + 平台性接口

**日期**: 2026-04-24
**关联 Story**: B-016, B-017, B-018

### S8-01~03: Proto optional 字段清理
- **变更概述**: 移除 DrcMqttBroker.enable_tls 的 optional 关键字，改为普通 bool；同步 SDK protocol.go（`*bool` → `bool`）和 drcmodeenterlogic.go（移除 nil 检查）
- **变更文件**:
  - `app/djigateway/djigateway.proto` -- enable_tls: optional bool → bool
  - `app/djigateway/djigateway/djigateway.pb.go` -- gen.sh 重新生成
  - `app/djigateway/internal/logic/drcmodeenterlogic.go` -- 简化 EnableTLS 赋值
  - `common/djisdk/protocol.go` -- DrcMqttBroker.EnableTLS: *bool → bool
- **影响范围**: djigateway proto + djisdk protocol
- **验证结果**: go build 通过

### S8-04~05: SDK Client 方法 + protocol 结构体补齐
- **变更概述**: 补齐 16 个缺失的 Client 方法封装和 9 个新数据结构体，实现 Method 常量与 Client 方法 1:1 覆盖
- **变更文件**:
  - `common/djisdk/client.go` -- 新增 16 个方法：StopFlightTask、BatteryMaintenanceSwitch、CameraAim、CameraPhotoStop、CameraPointFocusAction、CameraScreenSplit、CameraPhotoStorageSet、CameraVideoStorageSet、CameraLookAt、CameraScreenDrag、CameraIrMeteringPoint、CameraIrMeteringArea、LiveLensChange、ReturnSpecificHome、OtaCreate、SimulateMission
  - `common/djisdk/protocol.go` -- 新增 CameraPointFocusActionData、CameraScreenSplitData、CameraStorageSetData、CameraScreenDragData、CameraIrMeteringPointData、CameraIrMeteringAreaData、LiveLensChangeData、ReturnSpecificHomeData 共 9 个结构体
- **影响范围**: djisdk 包
- **验证结果**: go build 通过

### S8-06~07: 新增 6 个 gRPC RPC + Logic 实现
- **变更概述**: 新增 6 个 RPC 接口（5 个大疆标准协议转发 + 1 个平台能力），含完整 proto/server/logic 实现
- **变更文件**:
  - `app/djigateway/djigateway.proto` -- 新增 6 个 RPC + 4 个消息类型（DeviceOnlineRes, ReturnSpecificHomeReq, LiveLensChangeReq, OtaCreateReq + OtaDevice）
  - `app/djigateway/djigateway/djigateway.pb.go` -- gen.sh 重新生成
  - `app/djigateway/djigateway/djigateway_grpc.pb.go` -- gen.sh 重新生成
  - `app/djigateway/internal/server/djigatewayserver.go` -- 6 个 Server 方法新增
  - `app/djigateway/internal/logic/isdeviceonlinelogic.go` -- 查询 onlineCache
  - `app/djigateway/internal/logic/stopflighttasklogic.go` -- 调用 SDK StopFlightTask
  - `app/djigateway/internal/logic/returnspecifichomelogic.go` -- proto → SDK 结构体转换
  - `app/djigateway/internal/logic/batterymaintenanceswitchlogic.go` -- 调用 SDK BatteryMaintenanceSwitch
  - `app/djigateway/internal/logic/livelenschangelogic.go` -- proto → SDK 结构体转换
  - `app/djigateway/internal/logic/otacreatelogic.go` -- OtaDevice 列表转换 + 调用 SDK
- **影响范围**: djigateway 服务 gRPC 接口层 + djisdk 包
- **验证结果**: go build + go mod tidy + go vet 全部通过

### ⚠️ S8-09: 事故修复（第一轮）— 接口分类错误纠正
- **事故描述**: PM 将需求 3「平台性接口」理解为"补齐遗漏的下行指令 RPC"，实际应为"大疆 API 没有的、由本服务结合业务自定义的接口"。5 个指令类 RPC（StopFlightTask 等）代码本身正确（均对应 DJI Cloud API 文档中的 method），但被错误归类到「平台能力接口」分区
- **根因**: PM 对"平台能力"概念理解偏差
- **修复内容**:
  1. 开发计划新增「设计前提（铁律）」规则
  2. proto 注释分类初步修正
  3. Backlog B-018 描述修正
- **变更文件**:
  - `CP-开发流程/DJI 上云前置服务/开发计划.md` -- 新增设计前提（铁律）
  - `app/djigateway/djigateway.proto` -- 接口分类注释初步修正
  - `CP-开发流程/DJI 上云前置服务/Backlog.md` -- B-018 描述修正

### S8-10: 事故修复（第二轮）— Proto 分类按大疆文档归位 + 平台能力定义纠正
- **事故描述**: 老板二次指出：1) proto 分类不应自创「指令透传补齐」分区，应按大疆文档官方分类归组；2) 平台能力的定义不限于查询，是大疆 API 没有的、我们自己结合业务自定义组合的接口
- **修复内容**:
  1. proto RPC 分类修正：StopFlightTask 归入航线管理、ReturnSpecificHome 归入航线管理（与 ReturnHome 同组）、BatteryMaintenanceSwitch 归入远程调试、LiveLensChange 归入直播管理、OtaCreate 新增固件管理分区
  2. proto 消息定义分区修正：ReturnSpecificHomeReq/LiveLensChangeReq/OtaCreateReq 从「平台能力请求/响应消息」移至各自对应分区
  3. 平台能力区仅保留 IsDeviceOnline，标题改为「平台能力接口（非大疆标准 API，本服务自定义）」
  4. 开发计划「设计前提（铁律）」重写：纠正平台能力定义（不限于查询），新增「按大疆文档分类优先」规则，补充正反示例
  5. 开发计划接口设计节与 proto 保持一致
- **变更文件**:
  - `app/djigateway/djigateway.proto` -- RPC 分类归位 + 消息分区修正
  - `app/djigateway/djigateway/djigateway.pb.go` -- gen.sh 重新生成
  - `app/djigateway/djigateway/djigateway_grpc.pb.go` -- gen.sh 重新生成
  - `app/djigateway/internal/server/djigatewayserver.go` -- gen.sh 重新生成
  - `CP-开发流程/DJI 上云前置服务/开发计划.md` -- 设计前提重写 + 接口设计节修正
- **教训**: proto 分类优先按大疆文档，禁止自创分类；平台能力 = 大疆 API 没有的自定义接口，不限于查询

---

## Sprint S7: gRPC 错误码优化 + 机巢在线状态管理

**日期**: 2026-04-24
**关联 Story**: B-014, B-015

### S7-01: 新建 DJI 错误码映射工具
- **变更概述**: 定义 DJIError 结构化错误类型，从 proto 注释自动生成 449 条中文错误描述映射表
- **变更文件**:
  - `common/djisdk/error.go` -- 新增 DJIError 结构体 + NewDJIError/IsDJIError 工具函数
  - `common/djisdk/error_descriptions.go` -- 自动生成的 DJI 错误码中文描述映射表
- **影响范围**: djisdk 包
- **验证结果**: go build 通过

### S7-02: SendCommand 返回 DJI 错误码详情
- **变更概述**: 设备返回非零 result 时返回 DJIError 而非普通 error
- **变更文件**:
  - `common/djisdk/client.go` -- SendCommand 中 result!=0 改为返回 NewDJIError(result)
- **影响范围**: djisdk 包，所有调用 SendCommand 的 Logic
- **验证结果**: go build 通过

### S7-03: Logic 层 CommonRes 错误码丰富
- **变更概述**: proto 新增 reason_code 字段（DJI 原始错误码），42 个 Logic 统一使用 errRes/okRes helper 函数
- **变更文件**:
  - `app/djigateway/djigateway.proto` -- CommonRes 新增 reason_code 字段
  - `app/djigateway/djigateway/djigateway.pb.go` -- gen.sh 重新生成
  - `app/djigateway/internal/logic/helper.go` -- 新增 errRes（DJI 错误填入 reason_code + 中文描述）和 okRes
  - `app/djigateway/internal/logic/*.go` -- 42 个 Logic 文件统一替换为 errRes/okRes
- **影响范围**: djigateway gRPC 服务全部接口
- **验证结果**: go build 通过

### S7-04: SDK 层新增 onStatus 钩子
- **变更概述**: Client 新增 onStatus 钩子字段、OnStatus 注册方法、HandleStatus 回调处理、replyStatus 自动回复
- **变更文件**:
  - `common/djisdk/client.go` -- 新增 onStatus 字段 + OnStatus() + HandleStatus() + replyStatus()
- **影响范围**: djisdk 包
- **验证结果**: go build 通过

### S7-05: SubscribeAll 新增 Status 主题订阅
- **变更概述**: SubscribeAll 新增 sys/product/+/status 主题订阅
- **变更文件**:
  - `common/djisdk/client.go` -- topics map 新增 StatusTopicPattern -> HandleStatus
- **影响范围**: djisdk 包，MQTT 订阅
- **验证结果**: go build 通过

### S7-06: 业务层 hooks/status.go 实现
- **变更概述**: Status 钩子接收设备上下线事件，用 collection.Cache 标记机巢在线（60s TTL）
- **变更文件**:
  - `app/djigateway/internal/hooks/status.go` -- 新增 OnStatus 闭包 + IsOnline 检查函数
  - `app/djigateway/internal/svc/servicecontext.go` -- 初始化 collection.Cache + 注册 Status 钩子
- **影响范围**: djigateway 业务层
- **验证结果**: go build 通过

### S7-07: OSD 钩子增加 cache TTL 刷新
- **变更概述**: OSD 钩子改为闭包模式，收到 OSD 数据时刷新机巢在线 cache TTL（辅助心跳）
- **变更文件**:
  - `app/djigateway/internal/hooks/osd.go` -- 改为闭包函数，接收 onlineCache 参数
  - `app/djigateway/internal/svc/servicecontext.go` -- OnOsd 传入 onlineCache
- **影响范围**: djigateway OSD 处理
- **验证结果**: go build 通过

### S7-08: 下发命令前在线校验拦截
- **变更概述**: SDK 层新增 SetOnlineChecker + SendCommand 前在线检查，离线快速拒绝
- **变更文件**:
  - `common/djisdk/client.go` -- 新增 onlineChecker 字段 + SetOnlineChecker() + SendCommand 前置检查
  - `app/djigateway/internal/svc/servicecontext.go` -- 注册 onlineChecker
- **影响范围**: djisdk 包，所有 SendCommand 调用
- **验证结果**: go build + go mod tidy + go vet 全部通过

---

## Sprint S6: Dock3 全量 gRPC 接口暴露

**日期**: 2026-04-24
**关联 Story**: B-009, B-010, B-011, B-012, B-013

### S6-01~05: Proto 定义 — 新增 29 个 RPC + 16 个消息类型
- **变更概述**: 在 djigateway.proto 中新增远程调试（15 RPC）、相机/云台控制（6 RPC）、直播管理（3 RPC）、航线补充（4 RPC）、属性设置（1 RPC），共 29 个 RPC 方法和 16 个请求消息类型
- **变更文件**:
  - `app/djigateway/djigateway.proto` -- 新增 29 个 RPC 方法定义 + 16 个消息类型（CancelFlightTaskReq, FlightTaskWithOptionsReq, BatteryStoreModeReq, AlarmStateSwitchReq, AirConditionerModeSwitchReq, CameraModeSwitchReq, CameraPhotoTakeReq, CameraRecordingStartReq, CameraRecordingStopReq, CameraFocalLengthSetReq, GimbalResetReq, LiveStartPushReq, LiveStopPushReq, LiveSetQualityReq, SetPropertyReq）
- **影响范围**: djigateway 服务 gRPC 接口层
- **验证结果**: protoc 编译通过

### S6-06: gen.sh 代码生成
- **变更概述**: 执行 gen.sh 生成 pb.go / grpc / server / logic 全套骨架代码
- **变更文件**:
  - `app/djigateway/djigateway/` -- pb.go + grpc 生成文件更新
  - `app/djigateway/internal/server/djigatewayserver.go` -- 29 个 Server 方法新增
  - `app/djigateway/internal/logic/` -- 29 个 Logic 骨架文件生成
- **影响范围**: djigateway 服务全层
- **验证结果**: gen.sh 执行成功

### S6-07: Logic 实现 — 远程调试 15 个
- **变更概述**: 实现 15 个远程调试 Logic：DebugModeOpen/Close、CoverOpen/Close/ForceClose、DroneOpen/Close、DeviceReboot、ChargeOpen/Close、DroneFormat/DeviceFormat、SupplementLightOpen/Close、BatteryStoreModeSwitchSwitch、AlarmStateSwitch、AirConditionerModeSwitch
- **变更文件**:
  - `app/djigateway/internal/logic/debugmode*.go` -- 调用 SDK DebugMode 方法
  - `app/djigateway/internal/logic/cover*.go` -- 调用 SDK Cover 方法
  - `app/djigateway/internal/logic/drone*.go` -- 调用 SDK Drone 方法
  - `app/djigateway/internal/logic/devicerebootlogic.go` -- 调用 SDK DeviceReboot
  - `app/djigateway/internal/logic/charge*.go` -- 调用 SDK Charge 方法
  - `app/djigateway/internal/logic/*formatlogic.go` -- 调用 SDK Format 方法
  - `app/djigateway/internal/logic/supplementlight*.go` -- 调用 SDK SupplementLight 方法
  - `app/djigateway/internal/logic/batterystoremodeswitchswitchlogic.go` -- 参数转换 + 调用 SDK
  - `app/djigateway/internal/logic/alarmstateswitchlogic.go` -- 参数转换 + 调用 SDK
  - `app/djigateway/internal/logic/airconditionermodeswitchlogic.go` -- 参数转换 + 调用 SDK
- **影响范围**: djigateway Logic 层
- **验证结果**: go build 通过

### S6-08: Logic 实现 — 相机/云台 6 个
- **变更概述**: 实现 6 个相机/云台 Logic：CameraModeSwitch、CameraPhotoTake、CameraRecordingStart/Stop、CameraFocalLengthSet、GimbalReset，每个都做 proto → SDK 结构体的参数转换
- **变更文件**:
  - `app/djigateway/internal/logic/cameramodeswitchlogic.go` -- CameraModeSwitchData 转换
  - `app/djigateway/internal/logic/cameraphototakelogic.go` -- CameraPhotoTakeData 转换
  - `app/djigateway/internal/logic/camerarecordingstart/stoplogic.go` -- Recording 数据转换
  - `app/djigateway/internal/logic/camerafocallengthsetlogic.go` -- FocalLength 数据转换
  - `app/djigateway/internal/logic/gimbalresetlogic.go` -- GimbalResetData 转换
- **影响范围**: djigateway Logic 层
- **验证结果**: go build 通过

### S6-09: Logic 实现 — 直播 3 个
- **变更概述**: 实现 3 个直播 Logic：LiveStartPush、LiveStopPush、LiveSetQuality，每个做 proto → SDK 结构体转换
- **变更文件**:
  - `app/djigateway/internal/logic/livestartpushlogic.go` -- LiveStartPushData 转换
  - `app/djigateway/internal/logic/livestoppushlogic.go` -- LiveStopPushData 转换
  - `app/djigateway/internal/logic/livesetqualitylogic.go` -- LiveSetQualityData 转换
- **影响范围**: djigateway Logic 层
- **验证结果**: go build 通过

### S6-10: Logic 实现 — 航线补充 4 个
- **变更概述**: 实现 4 个航线补充 Logic：CancelFlightTask（传入 flight_ids 列表）、PauseFlightTask、ResumeFlightTask、ExecuteFlightTaskWithOptions（支持断点续飞参数）
- **变更文件**:
  - `app/djigateway/internal/logic/cancelflighttasklogic.go` -- 传入 flight_ids 列表
  - `app/djigateway/internal/logic/pauseflighttasklogic.go` -- 仅需 device_sn
  - `app/djigateway/internal/logic/resumeflighttasklogic.go` -- 仅需 device_sn
  - `app/djigateway/internal/logic/executeflighttaskwithoptionslogic.go` -- FlightTaskPrepareData + BreakPoint 完整参数构建
- **影响范围**: djigateway Logic 层
- **验证结果**: go build 通过

### S6-11: Logic 实现 — 属性设置 1 个
- **变更概述**: 实现 SetProperty Logic，接收 JSON 字符串解析为 PropertySetData map[string]any，下发 property/set MQTT 消息
- **变更文件**:
  - `app/djigateway/internal/logic/setpropertylogic.go` -- JSON 反序列化 + 调用 SDK SetProperty
- **影响范围**: djigateway Logic 层
- **验证结果**: go build 通过

### S6-12: 编译验证
- **变更概述**: 全项目编译验证通过
- **变更文件**: 无额外变更
- **影响范围**: 全项目
- **验证结果**: go build ./... + go mod tidy + go vet 全部通过（djigateway 模块零警告）

---

## Sprint S5: PSDK 透传规范化 + 钩子日志规范化

**日期**: 进行中
**关联 Story**: B-007, B-008

> 当前 Sprint 进行中，变更记录待回填。

---

## Sprint S4: MQTT 消息分发修复

**日期**: 2026-04
**关联 Story**: B-006

### S4-01: 修复 MQTT topicTemplate 匹配错误
- **变更概述**: 修复 mqttx 包消息分发机制中的 topicTemplate 匹配错误，`topicTemplateFromMsg` 返回完整主题而非订阅通配模板，导致 dispatcher 匹配失败
- **变更文件**:
  - `common/mqttx/client.go` -- messageHandler 闭包传入订阅时的 topic 模板；processMessage 新增 topicTemplate 参数；删除 topicTemplateFromMsg 函数
  - `common/mqttx/dispatcher.go` -- onNoHandler 默认回调增加 Infof 日志
- **影响范围**: common/mqttx 包，恢复所有通配订阅消息处理（HMS / OSD / State / Events / ServicesReply / PropertySetReply）
- **验证结果**: go build 通过

---

## Sprint S3: DRC 指令飞行控制 + HMS/OSD/State 钩子 + 安全拦截

**日期**: 2026-04
**关联 Story**: B-001, B-002, B-003, B-004, B-005

### S3-01: 接入 DRC 指令飞行控制全套 gRPC 接口
- **变更概述**: 新增 11 个 RPC 方法（航线管理、DRC 飞行控制、Flyto 指令、一键起飞），6 个新消息类型，11 个 Logic 文件全部实现
- **变更文件**:
  - `app/djigateway/djigateway.proto` -- 新增 11 个 RPC 方法和 6 个消息类型，按 DJI Cloud API 官方分类归组
  - `app/djigateway/internal/logic/` -- 11 个 Logic 文件实现
- **影响范围**: djigateway 服务 RPC 层
- **验证结果**: go build 通过

### S3-02: HMS 健康告警钩子
- **变更概述**: 新增 HMS 健康告警上报的类型化钩子体系
- **变更文件**:
  - `common/djisdk/protocol.go` -- 新增 HmsEventData + HmsItem 数据结构
  - `common/djisdk/client.go` -- 新增 onHmsEventNotify 钩子 + dispatchTypedEvent 分支
  - `app/djigateway/internal/hooks/hms_event.go` -- 钩子实现
  - `app/djigateway/internal/svc/servicecontext.go` -- 注册钩子
- **影响范围**: djisdk 包 + djigateway hooks 层
- **验证结果**: go build 通过

### S3-03: OSD/State 设备属性上报钩子
- **变更概述**: 新增 OSD 和 State 设备属性上报的钩子，SubscribeAll 新增主题订阅
- **变更文件**:
  - `common/djisdk/client.go` -- 新增 onOsd/onState 钩子、HandleOsd/HandleState 方法、extractDeviceSnFromTopic 辅助函数
  - `app/djigateway/internal/hooks/osd.go` -- OSD 钩子实现
  - `app/djigateway/internal/hooks/state.go` -- State 钩子实现
  - `app/djigateway/internal/svc/servicecontext.go` -- 注册钩子
- **影响范围**: djisdk 包 + djigateway hooks 层
- **验证结果**: go build 通过

### S3-04: 敏感接口配置拦截
- **变更概述**: 新增 DroneEmergencyStop 敏感接口的配置开关拦截机制
- **变更文件**:
  - `app/djigateway/internal/config/config.go` -- 新增 DangerousOpsConfig 结构体
  - `app/djigateway/internal/logic/droneemergencystoplogic.go` -- 入口检查开关
  - `app/djigateway/etc/djigateway.yaml` -- 默认关闭配置
- **影响范围**: djigateway 配置 + Logic 层
- **验证结果**: go build 通过

### S3-05: 接口归类修正
- **变更概述**: 按上云 API 官方分类修正 RPC 方法分组，return_home 系列从 DRC 移至航线管理
- **变更文件**:
  - `app/djigateway/djigateway.proto` -- RPC 分组注释修正
  - `common/djisdk/method.go` -- 常量分组注释同步修正
- **影响范围**: djigateway proto + djisdk method 常量
- **验证结果**: go build 通过

---

## Sprint S2: 事件钩子体系 + hooks 目录建设

**日期**: 2026-04
**关联 Story**: 首次开发

### S2-01: SDK 类型化事件钩子体系
- **变更概述**: 建立 SDK 类型化事件钩子体系，区分 up/down 消息方向，新建 gateway 服务 hooks 目录
- **变更文件**:
  - `common/djisdk/protocol.go` -- 新增 4 个设备上报事件结构体及子结构体
  - `common/djisdk/client.go` -- 新增 4 个类型化钩子字段和注册方法、dispatchTypedEvent 分发方法、重构 HandleEvents
  - `app/djigateway/internal/hooks/` -- 新建目录，每个事件钩子一个文件
  - `app/djigateway/internal/svc/servicecontext.go` -- 内联匿名函数替换为 hooks.OnXxx 引用
  - `app/djigateway/djigateway.proto` -- 补全完整中文注释
- **影响范围**: djisdk 包 + djigateway hooks 目录 + proto 注释
- **验证结果**: go build 通过

---

## Sprint S1: SDK 迁移与协议完善

**日期**: 2026-04
**关联 Story**: 首次开发

### S1-01: SDK 迁移至公共层 + 协议完善
- **变更概述**: 将 DJI SDK 从 gateway 内部迁移至公共层，全面完善 DJI Cloud API 协议覆盖
- **变更文件**:
  - `common/djisdk/protocol.go` -- 完善全部 DJI Cloud API 数据结构（60+ 字段中文注释）
  - `common/djisdk/method.go` -- 补全 60+ 方法常量
  - `common/djisdk/topic.go` -- 22 个主题生成函数
  - `common/djisdk/client.go` -- 40+ 客户端方法
  - `app/djigateway/djigateway.proto` -- PSDK 接口更新
  - `app/djigateway/internal/logic/sendcustomdatatopsdklogic.go` -- 接口重命名
- **影响范围**: common/djisdk 包（整体迁移）+ djigateway proto + logic
- **验证结果**: go build 通过
