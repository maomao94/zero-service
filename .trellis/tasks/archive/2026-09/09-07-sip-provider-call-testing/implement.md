# 实现计划

1. 扩展前端状态模型和 API 类型，补充供应商状态/表单需要的字段类型。
2. 将大厅整理为工作台页签，并接入供应商管理面板。
3. 抽取/增强拨号面板，使其在大厅和会议管理中都显示供应商选择并支持可选会议号。
4. 增加响应式样式，确保供应商表格、表单和拨号区在窄屏可用。
5. 执行 `npm run build`、`git diff --check`，检查未处理的异步错误和敏感字段泄露。
