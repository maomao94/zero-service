# 实施计划

- [ ] 校对 `HmsResolver.findTranslations` 的设备域和空中 Key 回退行为。
- [ ] 校对 `renderHmsTemplate`，保持 component index 通用加一，不增加产品限制。
- [ ] 修正 `HmsItem.Imminent` 注释。
- [ ] 扩展 `hms_test.go` 的 Key、填充和异常边界表格测试。
- [ ] 运行 `gofmt`、`go test ./common/djisdk`、`git diff --check`。
