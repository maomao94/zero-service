// Code generated by goctl. Safe to edit.
// goctl 1.9.2
// NOTE: SSE 路由已手动添加 rest.WithSSE()

package handler

import (
	"net/http"

	sse "zero-service/aiapp/ssegtw/internal/handler/sse"
	ssegtw "zero-service/aiapp/ssegtw/internal/handler/ssegtw"
	"zero-service/aiapp/ssegtw/internal/svc"

	"github.com/zeromicro/go-zero/rest"
)

func RegisterHandlers(server *rest.Server, serverCtx *svc.ServiceContext) {
	// SSE 路由组 - 事件流（长连接，需 rest.WithSSE()）
	server.AddRoutes(
		[]rest.Route{
			{
				// AI对话流
				Method:  http.MethodPost,
				Path:    "/chat/stream",
				Handler: sse.ChatStreamHandler(serverCtx),
			},
			{
				// SSE事件流
				Method:  http.MethodPost,
				Path:    "/stream",
				Handler: sse.SseStreamHandler(serverCtx),
			},
		},
		rest.WithPrefix("/ssegtw/v1/sse"),
		rest.WithSSE(),
	)

	// 普通路由组 - 健康检查
	server.AddRoutes(
		[]rest.Route{
			{
				// ping
				Method:  http.MethodGet,
				Path:    "/ping",
				Handler: ssegtw.PingHandler(serverCtx),
			},
		},
		rest.WithPrefix("/ssegtw/v1"),
	)
}
